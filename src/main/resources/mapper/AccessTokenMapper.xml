<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tool.mapper.AccessTokenMapper">

    <resultMap id="accessTokenResultMap" type="com.example.tool.entity.AccessToken">
        <id property="id" column="id"/>
        <result property="tokenValue" column="token_value"/>
        <result property="ownerName" column="owner_name"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="expiresAt" column="expires_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.tool.entity.AccessToken"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO access_token (token_value, owner_name, status, created_at, expires_at)
        VALUES (#{tokenValue}, #{ownerName}, #{status}, #{createdAt}, #{expiresAt})
    </insert>

    <select id="findById" resultMap="accessTokenResultMap">
        SELECT id, token_value, owner_name, status, created_at, expires_at
        FROM access_token
        WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="accessTokenResultMap">
        SELECT id, token_value, owner_name, status, created_at, expires_at
        FROM access_token
        ORDER BY id
    </select>

    <update id="update" parameterType="com.example.tool.entity.AccessToken">
        UPDATE access_token
        SET owner_name = #{ownerName},
            status      = #{status},
            expires_at  = #{expiresAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM access_token
        WHERE id = #{id}
    </delete>

</mapper>
